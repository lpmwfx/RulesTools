{
  "source": "crates/scanner/src/checks/topology_suffix.rs",
  "items": [
    {
      "name": "check",
      "kind": "Fn",
      "signature": "pub fn check( file_ctx: &FileContext, lines: &[&str], _cfg: &Config, issues: &mut Vec<Issue>, path: &Path, )",
      "line": 25,
      "doc": "Check Level 3 topology suffix: public types must carry a layer suffix.\n\n`pub struct Engine_core` in `src/core/` → OK\n`pub struct Engine` in `src/core/` → Error"
    }
  ]
}